# No env vars needed for the client — the nginx proxy handles /api routing.
# Vite dev server uses the proxy defined in vite.config.ts.
# For production builds behind a custom domain, you can set:
# VITE_API_URL=https://api.yourdomain.com
